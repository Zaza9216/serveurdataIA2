version: '3.3'

services:
  dataapi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataapi
    ports:
      - "80:80"
    environment:
      - NAME=World
    volumes:
      - .:/DataAPI
    networks:
      - app-network

networks:
  app-network:
